# OpenAI API Configuration
OPENAI_API_KEY=sk-your-api-key-here

# AWS S3 Vectors Configuration
AWS_REGION=us-east-1
S3_VECTORS_BUCKET=ribs-gift-recommendations
S3_VECTORS_INDEX=gifts

# Server Configuration
MCP_SERVER_NAME=gift-recommendations
LOG_LEVEL=INFO

# Transport Configuration (optional)
# MCP_TRANSPORT=sse        # sse (default) or stdio
# MCP_HOST=127.0.0.1       # Host to bind to
# MCP_PORT=3001            # Port for SSE server

# Optional: AWS Credentials (if not using ~/.aws/credentials or IAM roles)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
